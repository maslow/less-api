{
  "name": "less-api-project",
  "private": true,
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Maslow/less-api.git"
  },
  "scripts": {
    "build": "lerna run build",
    "test": "lerna run test",
    "copy-readme": "npx copy-cli ./README.md ./packages/core",
    "prepublish": "npm run build && npm run test && yarn copy-readme"
  },
  "devDependencies": {
    "copy": "^0.3.2",
    "lerna": "^4.0.0"
  }
}
